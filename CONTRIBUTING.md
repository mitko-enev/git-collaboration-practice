# Contributing Guidelines

1. Pick an open Issue from the repository.
2. Create a branch for your task:
   git checkout -b feature/task-name
3. Make changes and commit often with clear messages:
   git commit -m "Add task input functionality"
4. Push branch and open a Pull Request (PR) to main.
5. Request at least one review before merging.
6. Never push directly to main.
